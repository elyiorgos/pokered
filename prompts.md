### dex text
Translate the following asm file content into Serbian, respecting the format. Key things to keep in mind:
- Each dex entry can have a maximum of six lines of text, denoted text, next, next, para, next, next, you are allowed to add lines to entries if need be, as long as the maximum number of lines and the format is respected.
- Each line of text can have a maximum of 20 characters (including quotation marks)
- #MON should not be changed in any situation
- #MON should always be counted as 7 characters instead of 4
- Each translated line should have the original text next to it as a comment starting with ;

### dex entries
Translate the following asm file content into Serbian, respecting the format. Change only strings. Key things to keep in mind:
- Each string can have a maximum of 13 characters (including quotation marks)
- Each string must end with an @ symbol before the final quotation mark
- Each translated line should have the original text next to it as a comment starting with ;

### pokemon names
Translate the following asm file content (pokemon names) into Serbian, respecting the format. Change only strings. Key things to keep in mind:
- Each pokemon name string can have a maximum of 12 characters (including quotation marks)
- Each pokemon name should be translated into a Serbian equivalent (fan localization)
- Example: NIDORAN♀ -> IGLORAN♀
- Example: BULBASAUR -> LUKOSAUR
- Example: ARBOK -> ARBOK (Arbok is kobra backwards)
- Example: EKANS -> AJIMZ (Zmija backwards, since ekans is snake backwards)
- Each translated name should have the original name and the reasoning for the translation next to it as a comment starting with ;

### text 1
Translate the following asm file content into Serbian, respecting the format. Change only strings. Key things to keep in mind:
- Each line of text can have a maximum of 20 characters (including quotation marks)
- #MON should not be changed in any situation
- #MON should always be counted as 7 characters instead of 4
- @ should never be changed
- <PLAYER> should never be changed and should be counted as 7 characters 
- Each translated line should have the original text next to it as a comment starting with ;

### text 2
Translate the following asm file content into Serbian, respecting the format. Change only strings. Key things to keep in mind:
- Each line of text can have a maximum of 19 characters (including quotation marks)
- Keep spacing consistent with the original
- #MON should not be changed in any situation
- #MON should always be counted as 7 characters instead of 4
- #DEX should not be changed in any situation
- #DEX should always be counted as 7 characters instead of 4
- <COLON> should never be changed
- <RIVAL> should never be changed
- <_CONT> should never be changed
- <TARGET> should never be changed and should always be counted as 10 characters
- <USER> should never be changed and should always be counted as 10 characters
- Any word longer than one character that's fully capitalized should be left unchanged
- @ should never be changed
- <PLAYER> should never be changed and should be counted as 7 characters 
- Each translated line should have the original text next to it as a comment starting with ;

### text 3